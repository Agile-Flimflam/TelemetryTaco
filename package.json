{
  "name": "telemetry-taco",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight telemetry tool designed to correlate Feature Usage with System Health in real-time",
  "scripts": {
    "dev": "bash start.sh",
    "start": "bash start.sh",
    "stop": "bash stop.sh",
    "dev:frontend": "pnpm --filter ./frontend dev",
    "dev:backend": "cd backend && poetry run python manage.py runserver",
    "dev:worker": "cd backend && poetry run celery -A core worker --loglevel=info",
    "services": "docker-compose up -d db redis",
    "services:stop": "docker-compose stop db redis",
    "migrate": "cd backend && poetry run python manage.py migrate",
    "build": "pnpm --filter ./frontend build",
    "build:frontend": "pnpm --filter ./frontend build",
    "lint": "pnpm --filter ./frontend lint",
    "lint:frontend": "pnpm --filter ./frontend lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

